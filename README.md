# pixies-soundscope
PiXies – SoundScope is a React + TypeScript web app that lets users explore and play tracks from the Mixcloud sound API. It focuses on clean, testable architecture, robust async behavior (debounce and cancellation), and a polished UX with recent searches, cursor-based pagination, and an animated central track view
